<%- include("templates/header") %>
<h1>Demonstarting a Injection Attack</h1>  
<p>This page verifies that during the extraction of the users, more than 1 user were to be extracted.</p>
<div>
    <p>Users Extracted due to wrong query:</p>
    <ul>
        <% let i=0; %>
		<% for(i=0; i < users.length; i++) { %>
        <%- include('templates/user', {user: users[i]})  %> 
        <% } %> 
    </ul>
</div>
<%- include("templates/footer") %>